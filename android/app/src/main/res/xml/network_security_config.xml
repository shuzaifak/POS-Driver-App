<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">fonts.googleapis.com</domain>
        <domain includeSubdomains="true">fonts.gstatic.com</domain>
        <!-- Add your backend API domain here -->
        <domain includeSubdomains="true">your-api-domain.com</domain>
    </domain-config>

    <!-- For development, you can temporarily allow all cleartext traffic -->
    <!-- Remove this in production -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>